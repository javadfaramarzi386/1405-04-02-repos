# forum/apps.py

# کلاس پایه AppConfig برای تنظیمات اپلیکیشن در جنگو
from django.apps import AppConfig


class ForumConfig(AppConfig):
    """
    تنظیمات اپلیکیشن forum

    این کلاس به جنگو کمک می‌کند تا اپلیکیشن forum را
    در زمان اجرای پروژه شناسایی و مقداردهی اولیه کند.

    همچنین محل مناسبی برای:
    - بارگذاری سیگنال‌ها (signals)
    - تنظیمات اولیه اپلیکیشن
    - اتصال سرویس‌های جانبی هنگام startup
    """

    # تعیین نوع پیش‌فرض کلید اصلی مدل‌ها
    # BigAutoField باعث جلوگیری از محدودیت عددی در IDها می‌شود
    default_auto_field = 'django.db.models.BigAutoField'

    # نام فنی اپلیکیشن (باید با نام پوشه اپ برابر باشد)
    name = 'forum'

    # نام نمایشی اپلیکیشن در پنل مدیریت جنگو
    verbose_name = "فروم تبادل نظر"

    def ready(self):
        """
        این متد هنگام بالا آمدن پروژه جنگو اجرا می‌شود.

        کاربردهای رایج:
        - import کردن signals
        - اجرای تنظیمات اولیه اپلیکیشن
        - اتصال event listenerها

        نکته:
        این متد فقط یک‌بار هنگام startup اجرا می‌شود.
        """

        # در حال حاضر استفاده‌ای ندارد
        # اما محل استاندارد برای فعال‌سازی سیگنال‌ها در آینده است
        pass